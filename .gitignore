node_modules
dist
.astro
.env
.env.*
!.env.example
.DS_Store
.vercel
.netlify
.cache

# Runtime booking data (the JSON-file persistence is a placeholder for a
# real backend — see src/lib/availability.ts).
src/data/bookings.json
